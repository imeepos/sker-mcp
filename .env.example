# Sker Daemon MCP 服务器环境配置示例
# 复制此文件为 .env 并根据需要修改配置

# === 基础配置 ===
# 项目主目录（默认: ~/.sker）
SKER_HOME_DIR=~/.sker

# 应用环境（development、production、test）
NODE_ENV=development

# 调试模式
DEBUG=false

# === 日志配置 ===
# 日志级别（error、warn、info、debug、trace）
LOG_LEVEL=info

# === 服务器配置 ===
# MCP 服务器名称
MCP_SERVER_NAME=sker-daemon-mcp

# MCP 服务器版本
MCP_SERVER_VERSION=1.0.0

# 传输协议（stdio、http）
MCP_TRANSPORT=stdio

# HTTP 服务器端口（仅当传输为 http 时使用）
HTTP_PORT=3000

# === 插件配置 ===
# 插件目录
PLUGIN_DIR=plugins

# 启用插件热重载
PLUGIN_HOT_RELOAD=true

# 插件隔离级别（none、basic、strict）
PLUGIN_ISOLATION=basic

# === 配置文件 ===
# 自定义配置文件路径
CONFIG_FILE=

# 启用配置热重载
CONFIG_HOT_RELOAD=true

# === 安全配置 ===
# API 密钥（如果使用 HTTP 传输）
API_KEY=

# 允许的来源（CORS，如果使用 HTTP 传输）
ALLOWED_ORIGINS=*

# === 性能配置 ===
# 请求超时时间（毫秒）
REQUEST_TIMEOUT=30000

# 最大并发请求数
MAX_CONCURRENT_REQUESTS=100

# === 开发配置 ===
# 启用性能监控
PERFORMANCE_MONITORING=false

# 启用详细错误堆栈
VERBOSE_ERRORS=true